import SwiftUI

@main
struct GenGrabberApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Gen Grabber")
                .frame(minWidth: 700, minHeight: 500)
        }
        .defaultSize(width: 900, height: 600)
    }
}
